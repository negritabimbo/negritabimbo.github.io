<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario para editar un registro</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Inserta aquí el CSS proporcionado arriba */
        body {
            background-color: #f0ead2; /* Fondo claro */
            color: #6c584c; /* Texto oscuro */
            font-family: Arial, sans-serif;
        }

        .container {
            background-color: #dde5b4; /* Fondo beige */
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #6c584c; /* Verde oliva */
        }

        .form-label {
            color: #6c584c; /* Verde oliva */
        }

        .form-control {
            border: 2px solid #a98467; /* Marrón */
            border-radius: 4px;
            box-shadow: inset 0 1px 3px rgba(0, 0, 0, 0.12);
        }

        .form-control:focus {
            border-color: #adc178; /* Verde oliva */
            box-shadow: 0 0 5px rgba(173, 193, 120, 0.5);
        }

        .btn-primary {
            background-color: #adc178; /* Verde oliva */
            border-color: #adc178;
        }

        .btn-primary:hover {
            background-color: #a98467; /* Marrón claro */
            border-color: #a98467;
        }

        .btn-secondary {
            background-color: #a98467; /* Marrón claro */
            border-color: #a98467;
        }

        .btn-secondary:hover {
            background-color: #adc178; /* Verde oliva */
            border-color: #adc178;
            
        }
        .btn-custom {
            background-color: #a98467; /* Marrón claro */
            border-color: #a98467;
            color: #f0ead2;
        }

        .btn-custom:hover {
            background-color: #adc178; /* Verde oliva */
            border-color: #adc178;
            color: #f0ead2;
        }
    </style>
</head>
<body>
    <% if(user) { %>
    <div class="container mt-5">
        <h1 class="mb-4">Formulario de Registro</h1>
        <form action="/editar/<%= user.id %>" method="POST" enctype="multipart/form-data">
            <input id = "id" name = "id" value = "<% user.id %>" hidden>
            <div class="mb-3">
                <label for="nombre" class="form-label">Nombre:</label>
                <input type="text" class="form-control" id="nombre" name="nombre" value="<%= user.nombre %>" onfocus="if(this.value == '<%= user.nombre %>') { this.value = ''; }" onblur="if(this.value == '') { this.value = '<%= user.nombre %>'; }">
            </div>
            <div class="mb-3">
                <label for="apellido" class="form-label">Apellido:</label>
                <input type="text" class="form-control" id="apellido" name="apellido" value="<%= user.apellidos %>" onfocus="if(this.value == '<%= user.apellidos %>') { this.value = ''; }" onblur="if(this.value == '') { this.value = '<%= user.apellidos %>'; }">
            </div>
            <div class="mb-3">
                <label for="correo" class="form-label">Correo Electrónico:</label>
                <input type="email" class="form-control" id="correo" name="correo" value="<%= user.correo %>" onfocus="if(this.value == '<%= user.correo %>') { this.value = ''; }" onblur="if(this.value == '') { this.value = '<%= user.correo %>'; }">
            </div>
            <div class="mb-3">
                <label for="telefono" class="form-label">Teléfono:</label>
                <input type="tel" class="form-control" id="telefono" name="telefono" value = "<% user.telefono %>" onfocus="if(this.value == '<%= user.telefono %>') { this.value = ''; }" onblur="if(this.value == '') { this.value = '<%= user.telefono %>'; }">
            </div>
            <div class="mb-3">
                <label for="direccion" class="form-label">Dirección:</label>
                <input type="text" class="form-control" id="direccion" name="direccion" value = "<% user.direccion %>" onfocus="if(this.value == '<%= user.direccion %>') { this.value = ''; }" onblur="if(this.value == '') { this.value = '<%= user.direccion %>'; }">
            </div>
            <div class="mb-3">
                <label for="nota" class="form-label">Nota Adicional:</label>
                <input type="text" class="form-control" id="nota" name="nota" value = "<% user.nota %>" onfocus="if(this.value == '<%= user.nota %>') { this.value = ''; }" onblur="if(this.value == '') { this.value = '<%= user.nota %>'; }">
            </div>
            <div class="mb-3">
                <label for="apodo" class="form-label">foto:</label>
                <input type="file" class="form-control" id="foto"  name = "foto">
            </div>
            <div class="mb-3">
                <button type="submit" class="btn btn-primary me-3">Aceptar</button>
                <a href = '/admin' class="btn btn-custom" type="">Regresar a pagina de admin</a>
            </div>
        </form>
    </div>
    <% } %>
    <!-- Bootstrap JS (opcional, solo si necesitas funcionalidades como los componentes de Bootstrap) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        function cancelar() {
            // Aquí puedes agregar alguna lógica si deseas antes de cancelar, como limpiar los campos del formulario
            alert('Operación cancelada');
        }
    </script>
</body>
</html>
